# DSTT Kernel

A Python kernel for processing `.dstt` structure files with state tracking and multiple milestone processing.

## Version 1.0

Features:
- Nested DSTT definitions
- Extensible ToolProvider strategy decoupled from internal kernel code
- Initial state mapping and tracking through sub-transitions
- Automatic dependency extraction for parameters
